# Python Requirements for LLaMA 3 Conversion
# Install with: pip install -r requirements-conversion.txt

# Core dependencies
torch>=2.0.0
transformers>=4.40.0
numpy>=1.24.0
tqdm>=4.65.0

# Optional: for better performance
accelerate>=0.20.0
safetensors>=0.3.0

# Optional: for huggingface-cli download
huggingface_hub>=0.16.0
