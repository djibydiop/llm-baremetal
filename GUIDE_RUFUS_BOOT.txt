╔═══════════════════════════════════════════════════════════════════╗
║          GUIDE RAPIDE - FLASH USB AVEC RUFUS                     ║
╚═══════════════════════════════════════════════════════════════════╝

📋 PRÉREQUIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Clé USB (minimum 256 MB, sera EFFACÉE)
✓ Rufus ouvert
✓ Image: llm-baremetal-usb.img (dans ce dossier)
✓ PC avec Intel AX200/AX201/AX210 WiFi (pour test WiFi)

═══════════════════════════════════════════════════════════════════

🔧 ÉTAPES RUFUS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. SÉLECTION USB
   ┌─────────────────────────────────────┐
   │ Device: [Votre clé USB ▼]          │
   └─────────────────────────────────────┘
   → Choisir la clé USB dans le menu déroulant

2. SÉLECTION IMAGE
   ┌─────────────────────────────────────┐
   │ Boot selection: [SELECT]            │
   └─────────────────────────────────────┘
   → Cliquer "SELECT"
   → Naviguer vers: C:\Users\djibi\Desktop\baremetal\llm-baremetal\
   → Sélectionner: llm-baremetal-usb.img
   → "Ouvrir"

3. CONFIGURATION PARTITION
   ┌─────────────────────────────────────┐
   │ Partition scheme: GPT               │
   │ Target system: UEFI (non CSM)       │
   └─────────────────────────────────────┘
   → IMPORTANT: GPT + UEFI obligatoire!

4. OPTIONS (laisser par défaut)
   ┌─────────────────────────────────────┐
   │ Volume label: LLMBOOT (auto)        │
   │ File system: FAT32 (auto)           │
   │ Cluster size: Default               │
   └─────────────────────────────────────┘

5. LANCER
   ┌─────────────────────────────────────┐
   │              [START]                │
   └─────────────────────────────────────┘
   → Cliquer "START"
   → AVERTISSEMENT: "Toutes les données seront détruites"
   → Confirmer "OK"
   → Attendre 1-2 minutes

6. ÉJECTION
   ┌─────────────────────────────────────┐
   │ Status: READY                       │
   │              [CLOSE]                │
   └─────────────────────────────────────┘
   → Cliquer "CLOSE"
   → Éjecter proprement la clé USB

═══════════════════════════════════════════════════════════════════

⚙️  CONFIGURATION BIOS/UEFI
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Redémarrer le PC
2. Entrer dans le BIOS/UEFI (touche DEL, F2, F10, ou F12 selon PC)
3. Aller dans "Security" ou "Boot"
4. DÉSACTIVER "Secure Boot"
   → Secure Boot: [Disabled]
5. Configurer boot order:
   → USB en premier (ou F12 au boot pour menu)
6. Sauvegarder et quitter (F10)

═══════════════════════════════════════════════════════════════════

🚀 BOOT SUR LA CLÉ USB
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Insérer la clé USB
2. Redémarrer le PC
3. Appuyer sur F12/F11/Esc (menu boot) ou laisser booter auto
4. Sélectionner "USB Drive" ou "UEFI: [Nom de votre clé]"
5. Appuyer sur Entrée

═══════════════════════════════════════════════════════════════════

📺 CE QUE VOUS DEVRIEZ VOIR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE 1: Boot UEFI
  ┌──────────────────────────────────────┐
  │ Starting UEFI application...         │
  │ LLM Baremetal v1.0                   │
  └──────────────────────────────────────┘

PHASE 2: Détection Hardware
  ┌──────────────────────────────────────┐
  │ Scanning PCI devices...              │
  │ [00:14.3] Network Controller (Intel) │
  │ Device ID: 0x2723 (AX200)            │
  │ ✓ WiFi device detected!              │
  └──────────────────────────────────────┘

PHASE 3: Chargement Firmware
  ┌──────────────────────────────────────┐
  │ Loading firmware:                    │
  │   iwlwifi-cc-a0-72.ucode             │
  │ Size: 1329780 bytes                  │
  │ ✓ Firmware loaded successfully       │
  │ ✓ Firmware uploaded to device        │
  │ ✓ Firmware started (ALIVE received)  │
  └──────────────────────────────────────┘

PHASE 4: Activation Radio
  ┌──────────────────────────────────────┐
  │ Turning radio ON...                  │
  │ ✓ Radio enabled                      │
  │ Starting network scan...             │
  └──────────────────────────────────────┘

PHASE 5: Scan WiFi (SI TOUT VA BIEN)
  ┌──────────────────────────────────────┐
  │ Networks found:                      │
  │   1. MyWiFi_5G      [WPA2] -45 dBm   │
  │   2. Neighbor_Net   [WPA2] -67 dBm   │
  │   3. FreeWiFi       [Open] -72 dBm   │
  └──────────────────────────────────────┘

PHASE 6: Tentative de connexion (optionnel)
  ┌──────────────────────────────────────┐
  │ Connecting to: MyWiFi_5G             │
  │ Authentication: Open System          │
  │ Association: Sent                    │
  │ WPA2 4-way handshake...              │
  │ (peut timeout si EAPOL incomplet)    │
  └──────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════

❌ PROBLÈMES POSSIBLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. "No bootable device"
   → Secure Boot encore activé
   → USB pas en premier dans boot order
   → Image USB mal flashée

2. "WiFi device not found"
   → PC n'a pas de carte Intel AX200/AX201/AX210
   → Carte WiFi désactivée dans BIOS
   → Utiliser un PC avec Intel WiFi 6

3. "Firmware not found"
   → Firmware pas copié sur USB
   → Refaire: Rufus avec llm-baremetal-usb.img

4. "Firmware load failed"
   → Mauvais firmware (devrait être 1.27 MB)
   → Télécharger à nouveau

5. Écran noir
   → Attendre 30 secondes
   → Sortie série peut être nécessaire
   → Essayer un autre PC

═══════════════════════════════════════════════════════════════════

📸 PHOTO/VIDÉO RECOMMANDÉE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Prendre une photo ou vidéo de l'écran pour documenter:
  • Messages de boot
  • Détection WiFi
  • Liste des réseaux
  • Erreurs éventuelles

═══════════════════════════════════════════════════════════════════

🔍 DEBUGGING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Si besoin de logs détaillés:
1. Utiliser un câble série USB (optionnel)
2. Ou rediriger la sortie dans QEMU d'abord
3. Ou prendre des photos de l'écran

═══════════════════════════════════════════════════════════════════

✅ CHECKLIST FINALE AVANT BOOT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

□ Clé USB flashée avec Rufus (voir "READY")
□ Clé éjectée proprement de Windows
□ PC a Intel AX200/AX201/AX210 WiFi
□ Secure Boot DÉSACTIVÉ dans BIOS
□ Boot order: USB en premier (ou F12 pour menu)
□ Téléphone prêt pour photo/vidéo
□ GO!

═══════════════════════════════════════════════════════════════════
